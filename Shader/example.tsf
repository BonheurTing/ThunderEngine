Shader "TestShader"
{
    Properties
    {
    }
    SubShader
    {
        struct appdata
        {
            int4 vertex : POSITION;
            float2 uv : TEXCOORD0;
        };

        int frag(int x, int y)
        {
            for (int i = 0; i < 10; i++)
            {
                x += y;
            }
            frag(x,y);
            return x;
        }
    }
}